# M2 implementation status

Status: independent primitives implemented and tested. **Full M2 is incomplete.**
Tapweave is not yet playable and does not advertise production simulation or
replay capabilities. Follow the [M2 plan](m2-plan.md) for the remaining sequence.

This increment was developed separately while M1 was unfinished and imported
into the main worktree on 2026-09-12 after M1 completion. The [current status](../status.md)
records the completed M0/M1 gates. Adapting these independent primitives to the
final preparation/session contracts remains M2 work.

## Implemented

- Stable production result identifiers and explicit hit/miss, score, accuracy,
  combo, tick and bonus properties. Legacy padding result 99 is rejected.
- Allocation-free scoring over explicit judgement streams: perfect maxima,
  nested/ignored result counts, combo/highest combo, accuracy numerator and
  denominator, normalized score, midpoint-even rounding, rank and failure guard.
- Pinned hit-window predicates and forward-only spinner history preserving f32
  intermediates. These are primitives, not full circle/spinner state machines.
- Bounded drain-rate calibration over explicit maximum-health increases and
  break ends, preserving pinned operation order and search termination.
- Caller-buffer event heap and input ring: phase/component/sequence ordering,
  retained future events, output backpressure, transactional malformed/late batch
  rejection and consumed-slot reuse. These are not a complete session scheduler.
- Replay identity/frame validation, equal-time interpolation semantics and an
  allocation-free internal v2 codec with canonical little-endian values,
  full u64 sequences, checked sizes, reserved-byte validation and SHA-256.
  Corruption/failed decode preserves the caller's prior destination.
- Separate simulation trace schema/native/WASM transports and a real upstream
  reference adapter. Round-trippable float serialization avoids precision loss
  in the pinned Odin JSON library's default decimal formatting.

The codec carries a supplied final result digest; integrated simulation does not
recompute or verify that digest yet. It does not implement gameplay recording,
checkpoints, verified session playback or legacy `.osr`. See the
[internal replay contract](../../engine/replay/README.md).

## Original component evidence (2026-09-11)

On macOS arm64 with Node 24 and the pinned Odin compiler:

- 10 allocation-tracked Odin primitive test groups pass, including panic-allocator
  checks, transactional input errors, output capacity recovery, failure guard,
  replay identity rejection and every single-byte codec corruption.
- 96 generated fixtures produce byte-identical native/WASM traces. This includes
  local event ordering at direct/30/60/120/144 Hz and 50/100/250 ms stalls,
  replay sampling, and binary codec/checksum round trips.
- 67 fixtures compare exactly against actual pinned upstream components:
  result properties/score/rounding (H10 subset), hit windows (H06 subset), forward
  spinner history (H08 subset), and drain calibration (H09 subset).
- Full `npm --prefix engine test` passes the inherited foundation (14 Odin
  groups, 83 traces and lifecycle/C ABI probes), geometry (6 groups, 74 traces
  plus 4 failure cases), preparation (95 traces) and M2 regressions. Linux CI
  is configured but has not been executed by this local report.

The [finding index](../../engine/reference/findings/m2-primitives.json) records
source commits, lock hash, fixture/observation hashes, acceptance IDs and oracle
classification. Detailed inputs and observations regenerate under ignored
`engine/artifacts/simulation/`. Local event/replay fixtures are never labelled
upstream observations. No whole A13–A20 or A23 gate is marked complete.

Reproduce component comparisons with the clean pinned checkout/.NET environment
from the [reference-host README](../../engine/reference-host/README.md):

```sh
npm --prefix engine test
npm --prefix engine run test:simulation:upstream
```

## Main-worktree integration (2026-09-12)

Imported the independent M2 packages and merged scripts, CI, reference sources
and documentation into the completed M0/M1 checkout. Existing preparation,
runtime, ABI layouts and decoder implementation were preserved.

Validation in main passed `npm --prefix engine test` (including 88 decoder,
74 geometry plus 4 failure, 101 preparation and 96 M2 native/WASM fixtures) and
`npm --prefix engine run test:simulation:upstream` (67 exact pinned component
comparisons). The 10 allocation-tracked M2 test groups pass. Original finding
hashes remain historical evidence; regenerated full reports are under artifacts.

## Source findings and regression correction

**Circle deadline correction.** The earlier prose incorrectly made +400 ms the
automatic circle miss deadline. Pinned `OsuHitWindows.ResultFor` returns Miss
through ±400 ms; `CanBeHit` instead becomes false strictly after the Meh window.
`DrawableHitCircle` uses that predicate for automatic misses. H06 primitive
observations cover exact/adjacent boundaries; full drawable/input policy remains
unverified here. Reference prose and the roadmap boundary catalogue are corrected.

**Drain boundary caution.** Pinned calibration skips the interval when a break
end is crossed; gameplay derives broader no-drain periods from surrounding hit
objects. Default failure checking is not simply a continuous zero-crossing:
bonus maxima and IgnoreHit are exempt in `DrainingHealthProcessor`. H09's full
player/failure adapter remains necessary before wiring session failure events.

**Existing WASM regression order.** The copied preparation ABI test pre-grew
memory before the foundation detachment probe, causing its growth assertion to
fail without a foundation leak. Preparation now runs after the foundation
50-cycle growth/plateau assertion. Both tests retain their assertions.

## Remaining work and gates

| Remaining work | Required contract/evidence |
|---|---|
| Production map/session integration | Adapt to completed M1 identities, schedules, metadata and ownership; M1 exit evidence is recorded in current status |
| Full circles and note lock | Drawable dispatch, alive-object policy, forced miss order; H06/H07 |
| Full slider rules | M1 child/path contract plus tracking/catch-up/action-lock observations; H05/H07 |
| Full spinner judgements | M1 defaults plus cursor segments, density, threshold/bonus ordering; full H08 |
| Map-derived score maxima/hit events | Complete prepared judgement enumeration and session event metadata |
| Result health, drain intervals and terminal failure | M1 combo/break metadata and real player failure observations; full H09 |
| Sample/loop intent | M1 candidate contract and A20/H11 observations |
| Session recording/checkpoints/seek | Complete mutable session state and live/replay round-trip verification |
| Production ABI/snapshots/final results | ADR-005 refinement, concrete session contracts and generated records |
| Milestone performance/acceptance | Real simulation workloads and complete A13–A20/A23 schedule matrix |

Independent component evidence must not be promoted into full M2 completion.
No fixed 1 ms loop, browser callback gameplay, or substitute preparation was added.
